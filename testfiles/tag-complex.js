'use strict';
var servers = {
   development: 'development.example.com',
   production: 'production.example.com',
};

var compoundServer = 'http://' + /*server*/ '' /*/server*/ + '/api/services';

var server = /*server*/ servers.production /*/server*/ ;
var user = /*user*/ 'first.user' /*/user*/ ;
